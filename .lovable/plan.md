# Rebuild the Hotels Brand Blast 360 page

## Scope
- Rebuild only `/brand-blast-360/hotels` from the supplied HTML reference.
- Leave the other ten Brand Blast industry pages and all existing service/industry pages unchanged.

## Implementation
- Add a Hotels-only page component and route it before the shared `/brand-blast-360/:industry` page.
- Match the reference section order, outcome-led copy, dark navy/gold visual direction, responsive layout, and mobile sticky actions.
- Build the animated 988,000+ reach panel from real channel totals and respect reduced-motion settings.
- Use available hotel logo assets where they already exist; use clearly replaceable text marks for unavailable logos.
- Send all campaign actions to WhatsApp `94771976351`; send consultation actions to the existing contact page and preserve tracking attributes.

## SEO and discovery
- Set the exact requested title, description, self-canonical, one H1, four specified service H2s, internal links, breadcrumbs, FAQPage schema, and Service schema.
- Update only the Hotels static prerender content so crawlers receive the new Hotels page copy and metadata.

## Verification
- Check desktop and mobile layouts, section order, animation, CTA destinations, internal links, metadata, canonical, schemas, and browser errors.
- Confirm no other page files or content changed.
